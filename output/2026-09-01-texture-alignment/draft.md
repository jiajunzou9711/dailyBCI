# 2026-09-01 · DNN–脑对齐由纹理统计驱动（Loke et al., Current Biology 2026）

**张力类型：② 纠错型**（读者以为"DNN 能预测脑响应 = 两边在做同一件识别计算"，这篇削弱支持该说法的证据）
**主引论文**：Loke J, Sörensen LKA, Groen IIA, Cappaert N, Scholte HS. (2026). Shared texture-like representations underlie deep neural network alignment with human visual processing. Current Biology 36:1–8. doi:10.1016/j.cub.2026.08.008
**电极模态**：非侵入式 **EEG**，分析用 17 个后部电极（Oz、O1…）。人类，52 名被试进入分析。
**本地文件**：`papers/loke-2026-cub.pdf`（正刊定稿）、`papers/loke-2026-fulltext.txt`

## 拆篇（2026-09-01 用户定）
超过 18 张上限，拆成上下两篇。
- **上篇（本目录）**：卡①–⑩ + 落点卡 + 封面 + 目录卡 + 尾卡 ≈ 14 张。落点 = 对齐分数不由识别能力决定，问题被收敛成成分归属，实验这样搭。尾卡带下期预告。
- **下篇（另开目录）**：结果四节 + 闭环 + 引申边界 + 封面 + 目录 + 尾卡 ≈ 11–12 张。素材见本文件「备用素材」。

## 已定稿 · 上篇

### 卡① 网络与皮层的相似，比的是表征几何
过去十年有一条固定做法：把同一批图片喂给一个在 ImageNet 上训练好的深度网络，同时也给人或猕猴看，然后比较两边的**表征几何**——网络某一层对这批图的相互距离结构，与皮层对这批图的相互距离结构，有多像。像的程度叫**神经可预测性**（neural predictivity），也叫**对齐**（alignment）。

这条做法被当回事，靠的是一个推论：网络是为物体识别训练的，它能预测视觉皮层的响应，说明两边在做同一件计算——识别物体。这个推论有实证起点：Yamins 等人 2014 年在猕猴上发现，网络的物体识别性能与它对**单个 IT 单元**响应的预测力**强相关**¹。

这条推论后来遇到了两个不合拍的观测。

*（配图：暂无，考虑自制 RSA 示意图或用 Loke Fig 1A）*

### 卡② 两个观测撑不住「共享物体识别计算」
两个观测与这个解释不合。

第一，**把网络的识别准确率做上去，脑预测性并不跟着涨**，甚至反向²。

第二，**权重完全没训练过的网络，脑预测性也高于随机水平**³。识别能力是训练出来的，训练前不该具备。

这两处裂缝存在多年，通常被当作"指标不够灵敏"搁置。第一条有直接的实验证据。

*（配图：暂无）*

### 卡③ 识别准确率与脑预测力基本脱钩
两组独立工作先后测了这件事，方向一致。

Linsley 等人 2023 年用猕猴数据：两只猕猴，慢性植入 **32 通道多电极阵列**记录下颞叶（inferotemporal cortex, IT）神经元，按 Brain-Score 的标准流程评测 **135 个 DNN**。随 ImageNet 准确率上升，对 IT 的预测**变差**²。

Conwell 等人 2024 年用人类数据：**NSD 7T 功能磁共振**，**224 个模型**逐项受控对照。分类准确率与脑预测力之间**几乎没有关系**；把训练用的图固定住，换架构（CNN ↔ Transformer）或换训练目标（有监督分类 / 对比自监督 / 视觉—语言对齐），脑预测力也几乎不动⁴。

两者强度不同，一个是负相关、一个是无相关，但都指向同一件事：这个分数不由识别能力决定。第二处裂缝把这一点推得更远。

*（配图：暂无）*

〔出稿硬约束：**Linsley 是负相关、Conwell 是无相关，不得写成同一句话。**〕

### 卡④ 权重完全没训练的网络，也能预测出一部分脑响应
这里有两层拟合，只去掉了一层。

**网络权重的训练去掉了**——权重是随机初始化的，没学过任何东西。但**「网络中间层激活 → 脑响应」这一步映射仍然要拟合**，因为网络单元与神经元之间没有天然对应关系；评估仍在留出的图片与留出的被试上做。这样测出来，随机权重网络的脑预测性**高于随机水平**³。

强度要收准：**训练仍然重要**。Conwell 给每个受测架构都配了一个随机初始化的对照（**N = 64**），训练带来的提升是全篇最大、最稳的一项（cRSA **β = 0.30**、veRSA **β = 0.56**，均 p < 0.001）⁴。

所以这一条说的是：**架构本身、在任何学习发生之前，就已经能换到一部分非零的脑预测性**。习得的东西不该在训练前就有，这提示撑起对齐的成分与「学会识别物体」未必有关。

*（配图：暂无）*

### 卡⑤ 对齐分数是一个总量，问题在于哪个成分撑着它
两处裂缝合起来只证明了反面：对齐分数不由识别能力决定，也不完全依赖训练。它们没有回答这个分数由什么决定。

问题因此要换一种问法。脑对一批图片的响应里同时含着好几种成分——局部图像统计、物体形状、语义类别；网络的中间层激活里也同时含着这些成分。**那个分数是它们叠在一起算出来的一个总量。** 能问且可答的形式因此是：这个总量主要由哪一个成分撑着。

这是**成分归属**问题，与"像不像"的程度问题不同。它此前答不了，是因为在自然图片里这些成分绑在一起：同一张企鹅照片同时携带纹理统计与物体形状，无法从观测中分离这两者。

*（配图：暂无）*

### 卡⑥ 把两种成分拆开，看对齐跟着哪一个走
Loke 等人 2026 年把这个问题做成了可测的实验³。

论文的原问题是：DNN 与脑的对齐，反映的是两边共享对**物体相关信息**的敏感性，还是共享对**纹理类统计**的敏感性³。作者同时划了一条边界：早期与中级视觉皮层编码这类统计，这件事已确立；他们要问的是另一件事——**对齐反映的是神经响应里的哪一个成分**³。

押纹理统计这个候选，是因为两边各自被独立确立过在编码它。网络这边，Geirhos 等人 2019 年报告 ImageNet 训练的 CNN 存在 **texture bias**，分类时对纹理的依赖强于形状⁵；皮层这边，早期与中级视觉皮层编码纹理类统计⁶。没被测过的是这个共享的敏感性是否就是撑起对齐的那个成分。

自然图片里两种成分绑在一起，所以解法是人工造出**只保留其中一种**的图片，看对齐跟着哪一种走。**答案是纹理统计**³。

*（配图：Loke Fig 1A 或 1B）*

〔⚠ 边界（Step 6 查出）：texture bias 这一条**已被后续工作争议**。Burgert et al. (2025, NeurIPS Oral, arXiv:2509.20234) 用「系统性抑制形状/纹理/颜色线索」的框架重测，结论是 ImageNet 训练的 CNN **主要依赖局部形状**，Geirhos 的 cue-conflict 范式存在混淆。Loke 这篇未引该文。**本条只作为作者押纹理统计的动机来写、并注明是 Geirhos 的报告，不写成已确立的事实；本期结论不依赖它。**〕

### 卡⑦ 三种图片，每种只保留一部分成分
三行对应三种条件，同一列是同一张图的三个版本³。

**原图**（上排）：纹理统计、物体形状、背景三者齐全。
**纹理合成**（中排）：从白噪声出发，只匹配 **VGG-19 conv1_1 层的 Gram 矩阵**——即各通道响应之间的相关，也就是"局部统计的全局汇总"——最小化 Gram 矩阵的均方距离³。看第四列，企鹅的黑、白、黄三种材质与它们的空间尺度都留着，企鹅本身没有了。
**只留物体**（下排）：物体形状完整，背景去掉。同样看第四列，企鹅在，雪地没有了。

对齐分数在哪一排最高，就说明它由哪一种成分撑着。

*（配图：`papers/loke-fig1B.png`，Loke et al. 2026 Fig 1B）*

### 卡⑧ 被试只是看图，模型只是被跑一遍
两边都不做识别任务，这是设计的一部分³。

**人这边**：**52 人**进入统计（招募 57、排除 5，阿姆斯特丹大学，18–28 岁）。**200 张图**（取自 THINGS-EEG2 的留出测试集，200 个物体概念各一张）的三个版本，用 RSVP 呈现——每张图 **100 ms**，刺激起始间隔 **200 ms**，每张重复 20 次。任务与图片内容无关：注视十字有 **3%** 的时候由白变粉，被试按空格，其余时间什么都不做，图片一张接一张自己播完。被试从头到尾没有被问过任何关于图片的问题。这叫**正交任务**，作用是让人盯住中心、保持清醒。若任务改成"这是不是企鹅"，就等于人为诱导了物体识别加工³。

**模型这边**：网络在 ImageNet 上**预训练好、权重冻结**，测试时只把这 200 张图跑一遍、取各中间层的激活。5 个架构（AlexNet、VGG-16、ResNet-18、ResNet-50、ViT-B/16）共 21 个初始化，**所有层**都取。输出层不参与³。

两边各自的响应拿到手之后，还要换算成同一种可比的量。

*（配图：可用 Loke Fig 1C 的 RSVP 时序图）*

### 卡⑨ 两边都换算成「这 200 张图彼此有多不像」
换算的载体是**表征差异矩阵**（representational dissimilarity matrix, RDM）。

一张 200×200 的表，第 i 行第 j 列填"对第 i 张图的响应，与对第 j 张图的响应，有多不一样"，对角线为 0。网络单元与神经元之间没有一一对应关系，但"这 200 张图彼此有多不像"两边都能算，于是变得可比³。

脑这边用 **17 个后部电极**（Oz、O1…），每个时间点上一张图的响应就是一个 17 维振幅向量；每名被试、每个条件、每个时间点各得一张 RDM。模型这边对同样 200 张图逐层算 RDM。比较用 **weighted RSA + 岭回归**：把各层 RDM 加权组合去拟合 EEG 的 RDM，权重是学出来的；交叉验证留出 **15 名被试 + 100 张刺激**，重复 50 折。最终指标是 **0–500 ms 内 Pearson r 曲线下面积（AUC）**³。

这个 AUC 还要再除以一个上限才能读，那个上限叫噪声天花板。

*（配图：暂无，可考虑自制 RDM 示意图）*

### 卡⑩ AUC 要除以噪声天花板才能读
任何模型都有一个到不了的上限。

EEG 响应的总方差拆成三块：**所有被试共有的成分**、**个体特有的成分**、**测量噪声**（试次间抖动、信噪比有限）。模型对所有被试用的是同一套刺激特征，顶多能解释第一块；后两块在原理上就预测不到。第一块占的比例就叫**可解释方差**，也就是天花板³。

天花板只能估、不能直接测，所以它是一个**区间**，两端差的是"留出的那名被试算不算进平均里"³：
**上界** —— 每名被试的 RDM 与**包含他自己**的全体被试平均 RDM 的相关。他的数据在均值里，相关被抬高，属**高估**。
**下界** —— 与**不含他**的训练组被试平均 RDM 的相关。均值里少了他、被试数也更少，属**低估**。
真实天花板落在两者之间；Fig 2C 里那条灰色带子就是这个区间。

**这篇除的是上界**，也就是更大的那个数，属保守取法：报出来的比例读作"至少拿到这么多"。三种条件的天花板本身高低不同，不归一化就会把"这个条件天花板本来就低"误读成"模型在这个条件下更强"。

*（配图：Loke Fig 2C，灰带即噪声天花板区间）*

### 卡⑪ 答案是纹理统计，但要说清它否掉了什么
把成分拆开之后得到的答案：**对齐由局部图像统计的全局汇总撑着**，与"学会识别物体"未必有关³。

强度要收准。这削弱的是「对齐 ＝ 共享物体识别计算」这个**解释**，没有证伪「网络与脑存在共同之处」。共同之处确实存在，改写的是它的**成分归属**：两边对上的那一段，是早期的统计提取那一段。

上篇到此为止。支撑这个答案的四组证据——哪一种图片对齐最高、纹理特征能不能反过来预测原图的脑响应、物体信息能否从 EEG 解出来、对齐落在哪个时间窗——下一期逐条给。

*（配图：暂无）*

## 发布元数据（上篇 · 已定）
- **封面标题**：AI 像脑这件事 / 可能测错了地方
- **小红书发布标题**：`AI 越准越不像脑？神经网络与人脑的区别`
- **话题标签**：`#神经科学 #人工智能 #脑科学 #计算神经科学 #视觉皮层 #深度学习 #EEG #DailyBCI`
- **实采联想词（2026-09-01，搜「神经网络 大脑」）**：大家都在搜 = 神经网络可视化方法 / ai神经网络原理 / 大脑统一神经网络理论 / **神经网络与人脑的区别** / 大模型智能来自数据和神经网络 / 神经网络模拟大脑 / 神经网络是大脑的基础形态 / 神经网络与深度学习pdf / **神经网络和人类大脑的区别**；相关搜索 = 人脑神经网络模型原理 / **人脑和神经网络有什么区别**。搜「脑机接口」的联想词为就业/公司/股票向，与本期不匹配。
- **生成脚本**：`.claude/skills/dailybci/scripts/series/build_texture_align_cards.py` + `texture_align_figs.py`

## 下篇 TODO（素材见下方「备用素材」，papers/ 里的文件一律保留）
- [ ] 结果一：纹理条件对齐最高（85 / 44 / 55）—— 配图 Loke Fig 2A/2B
- [ ] 结果二：跨条件泛化（r = 0.20 @ 138 ms）—— 配图 Loke Fig 3
- [ ] 结果三：物体信息可解码但不驱动对齐 —— 配图 Loke Fig 4（四象限轨迹，本期最强的一张）
- [ ] 结果四：对齐集中在 100–200 ms 早期前馈 —— 配图 Loke Fig 2C
- [ ] 闭环：回到上篇两处裂缝
- [ ] 引申 + 强度边界 + 作者自列的四条局限
- [ ] 需保留的本地文件：`papers/loke-2026-cub.pdf`、`papers/loke-2026-fulltext.txt`、`papers/loke-fig1B.png`、`papers/lk-p2-hi.png`、`papers/lk-p{2,3,4,5}-lo.png`

## 讨论中
- [ ] 上篇封面标题 + 目录卡 + 尾卡（含下期预告）
- [ ] 下篇：结果四节 → 闭环 → 引申边界


## 备用素材（已从正刊核实）
- 被试：招募 57 人（45 女 12 男，18–28 岁，阿姆斯特丹大学），排除 5 人 → **统计样本量 52**；摘要与正文写"57 participants"，STAR Methods 的 Sample size 写 52。**出稿一律用 52，并说明招募 57。**
- 刺激：200 张图 = THINGS-EEG2 的留出测试集（200 个物体概念各 1 张）。三种版本：原图 / 纹理合成 / 只留物体（去背景）。
- 纹理合成：由白噪声出发，匹配 **VGG-19 conv1_1 的 Gram 矩阵**，最小化 Gram 矩阵均方距离。保留局部统计、破坏全局形状。
- 范式：RSVP，每图呈现 100 ms、刺激起始间隔 200 ms；任务是无关的注视点变色检测（3% 试次十字由白变粉，按空格）。每图 20 次重复。
- DNN：5 个架构 AlexNet / VGG-16 / ResNet-18 / ResNet-50 / ViT-B/16，共 21 个模型初始化；ImageNet 识别准确率跨度 AlexNet ~51% → ViT-B/16 ~81%。
- 对齐方法：weighted RSA + 岭回归，交叉验证留出 15 名被试 + 100 张刺激，重复 50 折；指标 = 0–500 ms 内 Pearson r 曲线下面积（AUC）。
- **主结果**：噪声天花板归一化后，纹理合成解释了最多 **约 85%** 的可解释方差，原图 **约 44%**，只留物体 **约 55%**（Fig 2B）。Friedman χ²(2)=37.68, p<0.001；纹理 vs 原图与 vs 只留物体两两 Wilcoxon 均 W=0, r=0.87, p<0.001（Bonferroni 校正）。
- 对齐峰值出现在刺激后 **100–200 ms**。200 ms 之后，原图与只留物体条件的噪声天花板更高，但 DNN 与之的差距更大。
- **跨条件泛化**（Fig 3）：预测原图 EEG 时，纹理特征的峰值对齐与原图特征相同（Pearson **r = 0.20，138 ms**），但原图特征 0–500 ms 的 AUC 显著更高（W=17, p<0.001, r=0.78）；原图特征也优于只留物体特征（W=1, p<0.001, r=0.87）。**纹理特征在预测原图 EEG 时甚至优于只留物体特征**（W=42, p=0.002, r=0.65）。
- **解码**（Fig 4）：单试次 EEG、17 个后部电极、多类 SVM 分 200 类，**随机水平 0.5%**。三种条件全部显著高于随机。Fig 4 横轴解码准确率范围约 0.6%–1.3%。
- **关键分离**：物体信息最多的两个条件（原图、只留物体）对齐反而最弱；纹理合成走进"高对齐 / 低物体信息"象限。
- 局限（作者自列）：① 100 ms RSVP + 无关任务可能偏向前馈处理；② 纹理合成同时打乱了背景与物体的纹理，无法分离两者贡献；③ 17 个后部电极偏向浅层视觉源，对前部下颞叶等深部结构不敏感（用全部电极排序不变，Fig S5）；④ 去掉全局形状同时去掉了语义内容，200 ms 后的自上而下反馈可能被削弱。

## 参考文献（按正文首次出现顺序，暂定编号）
¹ Yamins DLK, et al. (2014). Performance-optimized hierarchical models predict neural responses in higher visual cortex. PNAS 111:8619–8624.
² Linsley D, et al. (2023). Performance-optimized deep neural networks are evolving into worse models of inferotemporal visual cortex. NeurIPS 36:28873–28891.
³ Loke J, Sörensen LKA, Groen IIA, Cappaert N, Scholte HS. (2026). Shared texture-like representations underlie deep neural network alignment with human visual processing. Current Biology 36:1–8.
⁴ Conwell C, et al. (2024). A large-scale examination of inductive biases shaping high-level visual representation in brains and machines. Nature Communications 15:9383.
⁵ Geirhos R, et al. (2019). ImageNet-trained CNNs are biased towards texture; increasing shape bias improves accuracy and robustness. ICLR.
⁶ Jagadeesh AV, Gardner JL. (2022). Texture-like representation of objects in human visual cortex. PNAS 119:e2115302119.
