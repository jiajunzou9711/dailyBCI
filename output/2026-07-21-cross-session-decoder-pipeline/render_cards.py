#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""Render all 15 cards for 2026-07-21 methodology explainer issue."""

import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__),
                                "../../.claude/skills/dailybci/scripts"))
from card_generator import CardGenerator

OUT = os.path.dirname(os.path.abspath(__file__))
gen = CardGenerator(date="2026.07.21")

# 01 cover
gen.cover_card(
    '跨记录神经解码器',           # 跨记录神经解码器
    '怎么搭建',                                     # 怎么搭建
    '神经解码器换一次记录就退化，症结不在算法，在于每次记到的神经元不是同一批。\n本期以 POYO+ 为例，拆解这类跨记录解码器的设计逻辑。',
    os.path.join(OUT, '01-cover.png'),
    source='方法论解释期 · 以 Azabou et al. ICLR 2025 提出的 POYO+ 模型为参照'
)

# 02
gen.text_card(
    '解码器为什么换一天就退化',
    [
        '神经解码器把神经活动映射成行为变量。以运动解码为例：读运动皮层的动作电位（action potential），输出手的运动速度。',
        '它有个硬毛病——换一天、换一组电极、换一个被试，性能就明显下降。原因：电极每天记到的神经元不完全是同一批；就算是同一批，通道编号和神经元身份之间也没有固定对应；换个被试，脑区覆盖和植入位置全变。',
        '所以问题的根不在算法，在于每次的输入都对不上号。要理解怎么应对，先得看清它到底难在哪。',
    ],
    os.path.join(OUT, '02-problem.png'),
)

# 03
gen.text_card(
    '训练就是从候选集里挑一个',
    [
        '把学习剥到最素：训练 = 从一个候选集合（由模型结构划定的所有可能规则）里，用数据挑一个规则出来。',
        '挑得好不好，误差拆成两段：\n· 近似误差（approximation error）：集合里最接近真实规律的那个，离真实规律还有多远——取决于候选集合的大小\n· 估计误差（estimation error）：你实际挑到的，离集合内最优的那个有多远——取决于数据量',
        '关键在第二段——数据不够时，估计误差会失控。',
    ],
    os.path.join(OUT, '03-error-decomposition.png'),
)

# 04
gen.text_card(
    '失衡的比值',
    [
        '估计误差的量级，大致随**可训练参数 ÷ 数据量**这个比值走。',
        '为了让近似误差足够小，模型需要足够大的参数量——以 MOJO-POSSM 为例，可训练参数约 **760 万**¹。但一次校准（calibration）能拿到的配对数据只有几十个 trial（一次带标准答案的动作片段）。比值大到离谱，估计误差直接淹没一切。',
        '从零训练在这里不是效率问题，是信息量根本不够。出路只有一条：从这几十个 trial 之外，补信息进来。',
    ],
    os.path.join(OUT, '04-ratio-imbalance.png'),
)

# 05
gen.text_card(
    '预训练+微调，以及它的特殊障碍',
    [
        '补信息的办法，借自大语言模型：先在大量数据上预训练（pretrain）一个通用模型，再用少量新数据微调（fine-tune）。数据多的阶段撑得起大参数；数据少的阶段只微调一小部分。',
        '但神经数据有个大语言模型没有的障碍：每次记录的输入维度和含义都在变——神经元数目变、身份对不上、脑区都可能不同。传统模型第一层权重绑死了第 12 个输入，一旦第 12 个换了神经元，权重就是错的。',
        '于是真正要解决的问题变成：怎么让一个模型，不依赖具体是哪些神经元。',
    ],
    os.path.join(OUT, '05-pretrain-obstacle.png'),
)

# 06
gen.text_card(
    '把模型切成两半',
    [
        '答案是把模型沿变与不变的边界切开。',
        '**会变的**：每个神经元的身份——每次记录都是新的一批。\n**不变的**：一群神经元的活动如何随时间演化成一个动作——这条规律不绑定具体是哪些神经元。',
        '前者做小、每次重学；后者做大、一次学好反复用。接下来四块拼图，就是把这个切分落成一条真实的流水线。',
    ],
    os.path.join(OUT, '06-split-model.png'),
)

# 07
gen.text_card(
    '拼图① 查找表',
    [
        '先解决身份。做法是一张查找表（lookup table）：分选出多少个神经元，表就有多少行，每行是一串可学习的数字（示意为 64 个），描述这个神经元是一个什么样的单元。',
        '行的编号来自动作电位分选（spike sorting，按波形把电压归类、给每类一个序号）。这个序号完全任意、不携带信息，且跨记录不复用——这次的 12 号和下次的 12 号占两个不同的行，彼此无关。',
        '注意：一行只是这个神经元的身份描述，还没有时间信息。时间怎么进来，是下一块。',
    ],
    os.path.join(OUT, '07-lookup-table.png'),
)

# 08
gen.text_card(
    '拼图② 时刻：为什么要转角',
    [
        '一个动作电位发生在某个时刻，比如 0.10 秒。为什么不把 0.10 这个数直接喂进去？',
        '因为绝对时刻没有意义——换个计时起点，同样的活动就成了不同的数字；真正有信息的是动作电位之间的间隔。解法是旋转位置编码²（RoPE, rotary position embedding）：把时刻变成一个旋转角度（角度 = 固定频率 × 时刻）。这样任意两个动作电位比较时，结果只取决于它们的**时间差**，与从哪开始计时无关。',
        '那么旋转旋转的是什么？是查找表里那一行向量——下一张拆解。',
    ],
    os.path.join(OUT, '08-rope-why.png'),
)

# 09
gen.text_card(
    '拼图② 一个动作电位怎么来',
    [
        '一个动作电位的最终表示，就是把它所属神经元那一行向量，按它自己的时刻旋转一个角度。同一个神经元放三次电，就产生三个表示：同一行向量，被三个不同的时刻各转了不同的角。它们内容同源、只差一个旋转。',
        '高维旋转的做法是把这串数字两两配对成若干个平面（示意 64 维 → 32 个平面），每个平面各转各的、且用不同的转速，于是从毫秒到秒的多个时间尺度都能分辨。',
        '到这里，每个动作电位都成了一个带时间戳的向量。但它们的数量是不固定的——这才是下一个麻烦。',
    ],
    os.path.join(OUT, '09-rope-how.png'),
)

# 10
gen.text_card(
    '数量的麻烦',
    [
        '把一秒切成 50 个 20 毫秒的小窗（bin），每个小窗里发生多少个动作电位是不固定的——这个窗 7 个，那个窗 40 个。',
        '可后面负责理解时序的部分是一个固定结构的网络，输入尺寸必须固定。40 个、7 个地喂，它接不了。所以必须先把数量不固定的一堆向量，换成每个窗都一样多。',
        '而且不能靠求平均——那会把这几十个动作电位的差异全抹平。怎么压数量又不丢信息，是下一块。',
    ],
    os.path.join(OUT, '10-variable-count.png'),
)

# 11
gen.text_card(
    '拼图③ 把数量压成固定',
    [
        '手法叫交叉注意力（cross-attention）：预先备好固定数量的查询向量（示意为 16 个，训练学出、与窗里有多少动作电位无关）。',
        '每个查询对窗里每个动作电位打一个相关性分数，按分数**加权汇总**成一个结果。16 个查询就出 16 个结果——输出个数由查询定，**永远 16**，和输入有多少个无关。加权而非平均，所以数量被压掉、信息被重新组织成 16 个侧面而不是被抹平。',
        '一秒 50 个窗，每窗压成 16 个，就得到 **50 × 16 = 800** 个规整的向量，每个仍是 64 维（示意）。',
    ],
    os.path.join(OUT, '11-cross-attention.png'),
)

# 12
gen.text_card(
    '拼图④ 主干',
    [
        '这 800 个向量交给主干（backbone）——一个序列模型。前三块把每个瞬间的空间信息压好了，主干负责把 50 个窗连成一条时间演化。',
        '它让不同时刻的向量互相参考：一种做法是两两比较（self-attention），另一种是从头扫到尾、维护一个不断更新的状态（状态空间模型，如 Mamba）。做法不同，目的一样——让 50 个原本各自独立的窗，变成彼此知道对方、共同描述这一秒如何演化的整体。',
        '而这个主干，正是那个不变的一半。',
    ],
    os.path.join(OUT, '12-backbone.png'),
)

# 13
gen.text_card(
    '资产与耗材',
    [
        '现在能看清切分落到了哪。',
        '**主干是资产**：它学的群体活动如何随时间成为动作，不绑定具体神经元，跨记录、跨个体复用，部署时冻结不动。\n**查找表是耗材**：绑定这批神经元，换一次记录就作废重学。',
        '要说清一点——冻结的是如何算出时刻之间关系的规则（那些投影矩阵和转换参数），不是某一秒具体的关系；后者每次临场算出来。',
    ],
    os.path.join(OUT, '13-asset-consumable.png'),
)

# 14
gen.text_card(
    '校准没消失，被压缩了',
    [
        '这套设计没有消灭校准，而是把它从重训一个解码器缩小成给新神经元建行。',
        '新记录来了，只需给新神经元在查找表里建行、学这些行的数值，主干整个冻结。以 MOJO-POSSM（Mamba backbone）为例¹：在最难的迁移条件下（没见过的猴子做随机目标任务），这种只更新身份的微调，可训练参数不到 **1.8 万**，成绩却和全量重训（**760 万**可训练参数）持平。',
        '同样的表现，可训练参数差约两个数量级——这就是预训练+微调在神经数据上真正兑现的地方。',
    ],
    os.path.join(OUT, '14-calibration-compressed.png'),
)

# 15 tail
gen.tail_card(
    [
        '¹ Mao X, Krishna NH, Ryoo AH, Perich MG, Lajoie G. Leveraging unlabelled data for generalizable neural population decoding. arXiv 2607.14086, 2026. Table 1, monkey T-RT.',
        '',
        '² Su J, Ahmed MHM, Lu Y, Pan S, Bo W, Liu Y. RoFormer: Enhanced transformer with rotary position embedding. Neurocomputing 568:127063, 2024.',
        '',
        '模型参照: Azabou M, Pan KX, Arora V, Knight IJ, Dyer EL, Richards BA. Multi-session, multi-task neural decoding from distinct cell-types and brain regions. ICLR 2025 (POYO+).',
    ],
    os.path.join(OUT, '15-tail.png'),
    lead_paragraphs=[
        '这条流水线是 POYO 这一代模型的公共地基——把解码器的通用性，建立在把每次记录的差异在进主干之前就抹平上。地基之上最新的一步，是让它连没有行为标签的记录也能拿来预训练¹——那是另一期的事。',
    ]
)

print('Done — 15 cards rendered.')
