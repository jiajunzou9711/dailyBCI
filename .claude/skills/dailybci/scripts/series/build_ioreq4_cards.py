# -*- coding: utf-8 -*-
"""2026-09-18 · 电极上的电压，怎样成为文件里的数据（电生理 IO 系列 ep04）—— 卡片生成"""
import os, sys
HERE = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, os.path.dirname(HERE))
from card_generator import CardGenerator

PROJ = os.path.abspath(os.path.join(HERE, "..", "..", "..", "..", ".."))
EP = os.path.join(PROJ, "output", "2026-09-13-electrophysiology-io",
                  "ep04-2026-09-18-analog-input")
OUT = os.path.join(EP, "cards")
FIG = os.path.join(EP, "figs")
def f(n): return os.path.join(FIG, n + ".png")
def o(n): return os.path.join(OUT, n)

only = set(sys.argv[1:])
def want(name): return not only or name in only

gen = CardGenerator(date="2026.09.18")

if want("01-cover.png"):
    gen.cover_card(
        "电极上的电压，",
        "怎样成为文件里的数据",
        "这条链上每一步改变了什么，\n什么信息在这一步丢掉？",
        o("01-cover.png"),
        concept_image=f("cover-concept"), concept_height=400,
        title_size=74, title_top=90,
        source="本期不解读文献，从基础原理梳理模拟输入。",
    )

if want("02-toc.png"):
    gen.figure_card(
        f("toc"), "本期路线",
        ["一个电压要先指定相对哪个点测量，再放大、滤波，才进入采样与量化这两步不可逆的处理。",
         "最后把整数换算回微伏。每一步只问三件事：把什么变成了什么，哪些量由我们设定，什么信息丢掉了。"],
        o("02-toc.png"), figure_height=560,
    )

C = [
 dict(n="①", t="起点是连续的电压，终点是有限个整数", fig="fig1-chain", lab="图 1", fh=400,
      out="03-c1.png", a=[
   "电极两端有一个电压，它随时间变化的方式就是我们想研究的神经活动。记录要做的，是把这个电压变成文件里的数据。",
   "电压是连续的：任何时刻都有值，值可以是范围内的任意实数。文件里的数据是离散的：只有固定间隔的时刻有值，每个值只能取有限个档位之一。",
   "因此整条链最核心的事只有两件，**在时间上挑出若干时刻，在幅值上归到若干档位**，两件都不可逆。"]),
 dict(n="②", t="先说清楚测的是哪两个点之差", fig="fig2-two-refs", lab="图 2", fh=430,
      out="04-c2.png", a=[
   "电压是两点之差。说「电极上有一个电压」还不完整，必须指定第二个点。",
   "取设备内部的公共点、所有通道共用，这是**单端测量**（single-ended）；取另一个电极，这是**差分测量**（differential）。选法不同，量出来的就是不同的量。",
   "第二点一旦定下，后面每一步处理的都是这个差。"]),
 dict(n="③", t="不放大，整段信号落在一两个档位里", fig="fig3-gain-steps", lab="图 3", fh=450,
      out="05-c3.png", a=[
   "档位的宽度由输入范围和位数决定，与信号多大无关：16 位、±5 V 时约 153 µV 一档（按这两个参数算得）。",
   "电极上的神经信号在微伏到毫伏量级，不放大就整段落在一两档里，记下来近乎一条恒定值。放大一千倍，同一段信号覆盖几百个档位，波形才有形状。"]),
 dict(n="④", t="放大还让后级噪声相对变小", fig=None, out="06-c4.png", a=[
   "放大器之后的环节——连线、ADC——各自会加入噪声，加入的量由它们自己决定，与信号多大无关。先把信号抬高，这份噪声在结果里占的比例就小。器件手册把提高增益列为降低输入端等效噪声的手段，实测数据也是增益越大、等效噪声越低¹。",
   "增益换不来无限低的噪声。放大器自身输入端的噪声与信号一同被放大，折算回输入端不随增益改变，它构成下限。",
   "**放大不给信号增加信息。它做两件事：把信号撑开到覆盖足够多的量化档位；在后级噪声混入之前先把信号抬高。**"]),
 dict(n="⑤", t="增益过大，超出范围的部分被削平", fig="fig4-clip", lab="图 4", fh=400,
      out="07-c5.png", a=[
   "放大后的电压超出输入范围，超出部分全部输出同一个极限值，这叫**削顶**（clipping）。它突然发生，且无法还原。",
   "两种损失性质不同，选择因此不对称：增益按信号可能出现的**最大**幅度来定，让这个最大值落在输入范围以内，再给基线漂移和偶发的大幅波动留一段余量。"]),
 dict(n="⑥", t="同一条原理决定放大器放在哪里", fig="fig5-headstage", lab="图 5", fh=470,
      out="08-c6.png", a=[
   "线缆耦合进来的干扰，大小由外界环境和线缆决定，与线上的信号多大无关。",
   "把放大器做进紧贴电极的小型模块（动物实验中常用的 headstage 即属此类），为的就是让走原始信号的那一段尽可能短。"]),
 dict(n="⑦", t="滤波决定哪些频率进入后面的步骤", fig=None, out="09-c7.png", a=[
   "一段电压可以看成许多不同频率的成分叠加。滤波按频率削弱其中一部分：削弱低频的叫**高通**，削弱高频的叫**低通**。",
   "**高通要在放大之前。** 电极与组织接触时界面上形成一个稳定的电位差，与神经活动无关，称为**电极直流偏置**，它可以比神经信号大出几十倍以上。不先削掉，放大后立刻超出输入范围。Intan RHD2000 系列芯片就在输入端用内部电容阻断它²。",
   "**这一步与软件里滤波不同。** 它发生在数据写进文件之前，削掉的成分从未变成数字，事后无法恢复；软件滤波作用于已存下的数，只要原始数据还在就可撤。"]),
 dict(n="⑧", t="采样只在固定间隔的时刻取值", fig="fig6-sampling", lab="图 6", fh=380,
      out="10-c8.png", a=[
   "每秒取值的次数叫**采样率**（sampling rate）。采样率 1000 Hz，就是每毫秒取一个值，两次取值之间发生了什么，不进入记录。",
   "于是问题变成：每秒取多少次才够？要定出这个数字，得先知道「不够」会以什么形式表现出来。"]),
 dict(n="⑨", t="采样不够密，记录里会出现本不存在的频率", fig="fig7-alias", lab="图 7", fh=410,
      out="11-c9.png", a=[
   "采样率 1000 Hz 时，900 Hz 的成分在每毫秒的那些时刻上给出的数值，与 100 Hz 的成分完全相同，两者在记录中不可区分。这就是**混叠**（aliasing）。",
   "分界线在采样率的一半，称为**奈奎斯特频率**（Nyquist frequency）。高于它的成分不会消失，而是折回到它以下。"]),
 dict(n="⑩", t="抗混叠滤波为什么必须在采样之前", fig=None, out="12-c10.png", a=[
   "这是整条链上最硬的一处不可逆。记录里折回来的那个频率与真实的同频成分数值完全一样，事后无法分开；数字滤波也不行——它已经落在关心的频段里，删掉它会连真信号一起删。",
   "因此高于奈奎斯特频率的成分必须在采样之前用模拟低通削掉，这类低通称为**抗混叠滤波**（anti-aliasing filter）。放在采样之后没有意义。",
   "采样定理给出的条件是：函数若不含高于某个频率的成分，按该频率两倍的密度取值即可完全确定它³。实际设置要高于两倍，因为滤波无法在分界线上直接截断。"]),
 dict(n="⑪", t="量化把电压归到有限个档位", fig="fig8-quantize", lab="图 8", fh=400,
      out="13-c11.png", a=[
   "取到的每一个值仍是可以取任意值的电压，而文件里存的是有限位数的数，所以这一步必然把一批不同的电压表示成同一个数。",
   "做法是：规定一段输入范围，按位数切成等宽的小段，判断电压落在哪一段，输出那一段的编号。范围之外的电压没有对应编号，只能输出端点的那个，这就是削顶。"]),
 dict(n="⑫", t="实际能分辨多小，不由位数单独决定", fig=None, out="14-c12.png", a=[
   "当档宽已经远小于噪声的幅度时，同一个恒定的输入电压，读出来的整数会在相邻若干档之间随机跳动。一个变化必须大到超过噪声的抖动范围，才能判断它来自真实的输入。",
   "TI 的 ADS1299 是一款用于脑电记录的模数转换前端芯片。它的数据手册在一组常用设置下给出输入端等效噪声约 0.14 µV¹，而同一设置下档宽约 0.022 µV（按手册给的算式算得）。于是手册在标称的 24 位之外，另给一个按实际能相互区分的档数折算出的位数，约 18.5 位¹。",
   "**判断一份记录能分辨多小的变化，要把档宽与输入端等效噪声放在一起比，瓶颈在两者中较大的那一个。**"]),
 dict(n="⑬", t="把整数换算回微伏", fig=None, out="15-c13.png", a=[
   "文件里的整数只是档位的编号。要还原成电极上的电压，把这条链倒着走一遍：",
   "**电极端电压 = 整数 × 输入范围 ÷ 2^位数 ÷ 增益**",
   "采集软件通常已经替你做完这一步。要留意它用的是哪一组参数，尤其在增益改过、或同一台设备不同通道增益不同时。",
   "参数取错的后果很特别：波形的形状、频率、时间关系全部正确，只有幅度整体差一个倍数。它不像削顶或混叠那样留下可见的异常，因此最容易一路带进统计分析。"]),
 dict(n="⑭", t="三种典型失败，分别发生在哪一步", fig="fig9-failures", lab="表 1", fh=400,
      out="16-c14.png", a=[
   "三者的排序正好对应可补救的程度：换算错了能改，削顶和混叠只能重录。这也是增益与采样率必须在开始记录之前定好的原因。"]),
]

for c in C:
    if not want(c["out"]):
        continue
    if c["fig"]:
        gen.figure_card(f(c["fig"]), c["lab"], c["a"], o(c["out"]),
                        figure_height=c["fh"], title=c["t"], title_num=c["n"])
    else:
        gen.text_card(None, c["a"], o(c["out"]), heading_lines=[f'{c["n"]} {c["t"]}'])

REFS = [
  "1. Texas Instruments. (2017). ADS1299-x Low-Noise, 4-, 6-, 8-Channel, 24-Bit, Analog-to-Digital Converter for EEG and Biopotential Measurements Datasheet. SBAS499C.",
  "2. Intan Technologies. (2023). RHD2000 Series Digital Electrophysiology Interface Chips Datasheet.",
  "3. Shannon C. E. (1949). Communication in the Presence of Noise. Proceedings of the IRE 37(1):10-21.",
]

if want("17-tail.png"):
    gen.tail_card(REFS, o("17-tail.png"), lead_paragraphs=[
      "从电极上的一个电压到文件里的一个整数：指定测量的第二个点，放大到可测量的范围，按频率取舍，按固定间隔取值，归到有限个档位，再换算回物理单位。其中取点与归档不可逆，放大和滤波的作用是让这两件事发生在合适的范围内。",
      "采样率几乎总由使用者设定，滤波截止频率在许多前端上可设，增益视设备而定，位数和输入范围则多数固定。这条链的用处落在三件事上：选设备时看哪些指标、记录前怎么设参数、拿到数据后哪些异常还有救。",
    ])
print("done")
