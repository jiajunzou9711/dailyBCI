"""2026-07-31 原理期 — 跨 session 可辨识性. 17 张小红书图卡."""
import os, sys

HERE = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, os.path.dirname(HERE))
from card_generator import CardGenerator

PROJECT = os.path.abspath(os.path.join(HERE, "..", "..", "..", "..", ".."))
OUT = os.path.join(PROJECT, "output", "2026-07-31-identifiability")
FIGS = os.path.join(OUT, "figs")
os.makedirs(OUT, exist_ok=True)
F = lambda n: os.path.join(FIGS, n + ".png")
P = lambda n: os.path.join(OUT, n)

gen = CardGenerator(date="2026.07.31")

# 01 封面 -----------------------------------------------------------------
gen.cover_card(
    "多次记录拼起来",
    "有些量拼不回来",
    "跨神经元的协同波动，只在同时记录时才存在可测量的形式。",
    P("01-cover.png"),
    source=("本期讲一条统计学判据在神经数据分析中的位置：什么时候可以把多次记录拼起来，"
            "什么时候不能。文中全部数字为便于说明而构造的算例，非实测数据。"
            "涉及的方法学工作列于尾卡。"),
)

# 02 目录 -----------------------------------------------------------------
gen.figure_card(
    F("toc"), "本期路线",
    ["从记录手段的限制出发，一层一层推到一条能自己用的判据，"
     "最后回答跨 session 模型处在什么位置。"],
    P("02-toc.png"), figure_height=700,
)

# 03 困境 -----------------------------------------------------------------
gen.figure_card(
    F("fig-dilemma"), "记录手段的两端",
    ["同时观察多个脑区这件事本身并不难。",
     "如上图左侧，**EEG**（头皮脑电）、**MEG**（脑磁图）、**fMRI**（功能磁共振）、"
     "功能超声与贴在皮层表面的 **ECoG**，都能一次覆盖很大范围；它们记录到的是场电位或"
     "血流动力学信号，由大量神经元的活动汇总而成。右侧想拿到单个神经元的动作电位，"
     "目前要把电极扎进脑组织，而一次能同时读多少个位置，被记录通道数卡死。",
     "稀缺的是底部那三者的组合：**单神经元分辨率 × 多个脑区 × 同时**。"
     "领域绕过这个限制的办法，直接决定了后面能问什么问题。"],
    P("03-dilemma.png"), figure_height=620,
)

# 04 通行绕法 --------------------------------------------------------------
gen.text_card(
    "通行的绕法是分多次记录再拼起来",
    ["绕过通道数限制的通行做法，是把一次实验拆成多次。",
     "今天把电极放在这两个区、明天放在另外两个区，每次都让动物做同一个刻板化的任务；"
     "事后按任务事件把各次数据对齐，并成一个群体来分析。这个群体在物理上从未同时存在过，"
     "称作**伪群体**（pseudo-population）。对绝大多数实验室来说这是唯一可行的路，"
     "大量已发表的分析建立在它之上。",
     "真实的困惑也在这里：拼出来的群体，哪些分析结论站得住？本期给一条能自己判断的判据。"
     "开始之前，先把「拼」这个词拆开。"],
    P("04-workaround.png"),
)

# 05 两种拼 ---------------------------------------------------------------
gen.figure_card(
    F("fig-two-joins"), "两种「拼」",
    ["「把多次记录加起来」这句话，在不同语境下指两件不同的事。",
     "如上图左，第一种是**拼神经元**，把不同 session 的神经元并成一个群体再分析，"
     "也就是上一张说的伪群体。右边第二种是**拼结果**，每次记录各自算完、把数字加总；"
     "它在计数上是干净的，因为各次采到的神经元互不重叠，各自算出的结果集合也互不重叠。",
     "底部那一行是两者的共同点：**都无法恢复跨次的配对信息**。"
     "要看清这一点，得先说清一次记录到底给出了什么。"],
    P("05-two-joins.png"), figure_height=620,
)

# 06 随机变量 --------------------------------------------------------------
gen.text_card(
    "神经元的发放是随机变量",
    ["一次记录给出的东西，比「每个神经元放了多少电」更结构化。",
     "取一个观测窗口，比如某个事件之后的 100 毫秒。同一个实验条件重复很多试次，"
     "同一个神经元在这个窗口里发放的动作电位数每次都不一样：这次 3 个、下次 1 个、"
     "再下次 5 个。所以它的发放是一个**随机变量**；把它取各个值的概率列出来，"
     "就是这个神经元的**分布**。",
     "当一次记录同时采到两个神经元时，能拿到的东西比「两个分布」更多。"],
    P("06-random-variable.png"),
)

# 07 联合表 ---------------------------------------------------------------
gen.figure_card(
    F("fig-joint"), "联合分布",
    ["同时记录两个神经元，拿到的是它们取值的**配对**记录。",
     "如上图，把发放简化成二值后，同一条件做 1000 个试次、每次记下 (u, v) 这一对取值，"
     "排成一张 2×2 的表，这就是**联合分布**。表里含着一件只有同时记录才知道的事："
     "对角线上的 **400** 和 **400** 远大于非对角的 100 和 100，"
     "说明这两个神经元倾向于一起发放、也一起沉默。",
     "表中数字为便于说明而构造的算例。分次记录时，这张表你只能拿到它的边。"],
    P("07-joint.png"), figure_height=640,
)

# 08 边缘 -----------------------------------------------------------------
gen.figure_card(
    F("fig-marginal"), "边缘分布",
    ["分次记录能给出的，是这张表按行、按列求和之后的结果。",
     "如上图，每一行加起来写在右边、每一列加起来写在下边，四个 **500** 就是 u 和 v "
     "各自的**边缘分布**；「边缘」这个名字的字面出处，就是写在表格边上的那些和，"
     "求这些和的操作叫**边缘化**。中间四格标着问号，因为周一只记 u、周三只记 v 时，"
     "它们始终空着。",
     "你知道 u 有一半试次发放、v 有一半试次发放，**不知道它们是不是同一半试次**。"
     "那么，两条边能不能把内部的四格钉住？"],
    P("08-marginal.png"), figure_height=650,
)

# 09 三张表 ---------------------------------------------------------------
gen.figure_card(
    F("fig-three"), "同样的边缘，三种真相",
    ["两条边钉不住表格内部。",
     "如上图三张表，行和与列和都是 500，内部却截然不同：左边 400/100/100/400 是"
     "一起发放、一起沉默；中间 100/400/400/100 是一个发放时另一个沉默；"
     "右边四格均为 250，谁发放与另一个毫无关系。对应的神经机制分别是"
     "**共同驱动**、**相互抑制**、**彼此独立**。",
     "而分次记录得到的观测，在这三种情况下完全一样。"
     "这个差异到底能有多大，可以直接算出来。"],
    P("09-three-tables.png"), figure_height=640,
)

# 10 自由参数 --------------------------------------------------------------
gen.figure_card(
    F("fig-sweep"), "一个自由参数",
    ["边缘一旦固定，整张表只剩**一个**自由参数。",
     "如上图，设左上角那格是 a，其余三格随即确定为 500−a、500−a、a。"
     "此时 u、v 的均值与标准差都是 0.5，于是 **ρ = a / 250 − 1**。"
     "下方坐标轴上，a 扫过 0 到 500，ρ 就扫满 **−1 到 +1**。",
     "在这个算例里，边缘分布对 ρ 的约束等于零。一般情形下边缘会给 ρ 划出一个可行范围，"
     "这组界限叫 **Fréchet–Hoeffding 界**¹，而它给出的永远是一个区间。"
     "既然如此，多攒些试次能把它逼出来吗？"],
    P("10-sweep.png"), figure_height=620,
)

# 11 两类问题 --------------------------------------------------------------
gen.figure_card(
    F("fig-identify"), "两类问题",
    ["多攒试次消不掉这个不确定，因为它的性质与噪声不同。",
     "如上图，判据只有一句：有无限多试次、噪声完全被平均掉，这个不确定会消失吗？"
     "会消失的落在左边，是**估计问题**，真相唯一、只是被有限样本掩盖，"
     "更多数据和更好的模型都管用；不会消失的落在右边，是**可辨识性问题**，"
     "两个不同的真相产生完全相同的观测。",
     "上一张那三张表落在右边。分次记录一亿个试次，两条边会被估得极其精确，"
     "三张表依旧全部与观测相容。那么把数据交给更强的模型呢？"],
    P("11-identifiability.png"), figure_height=620,
)

# 12 后验=先验 -------------------------------------------------------------
gen.figure_card(
    F("fig-bayes"), "后验等于先验",
    ["更强的模型在这里不改变结论。",
     "如上图，后验正比于先验乘似然。左侧那条红色水平线表示**似然沿 a 是常数**："
     "a 取 0 到 500 之间的任何值，观测到这份数据的概率完全相同。于是右侧的结论成立，"
     "**后验等于先验**，数据对这个方向没有做任何更新。",
     "把分次记录的数据喂给一个在大量同时记录数据上训练过的模型，它会输出一个相关系数，"
     "那个数来自它见过的典型模式，这个操作叫**插补**，可以用来生成假说。"
     "那么分次记录的数据是不是就没有用了？"],
    P("12-bayes.png"), figure_height=620,
)

# 13 等价类 ---------------------------------------------------------------
gen.text_card(
    "可辨识与否，取决于你要问哪个量",
    ["同一份分次记录的数据，对一部分问题是充分的，对另一部分是零信息。",
     "把「数据分不出来的那些可能性」合成一组，称作一个**等价类**——在这个算例里，"
     "等价类就是 a 从 0 到 500 那一整条线。判据是：**一个量可辨识，"
     "当且仅当它在整个等价类上取常数。**",
     "对比就在同一张表里：相关系数 ρ = a/250 − 1 随 a 变化，从 −1 到 +1，**不可辨识**；"
     "而 u 的发放概率等于 (500−a) 与 a 相加再除以 1000，恒等于 **0.5**，**可辨识**，"
     "因为 a 在求和中被消掉了。边缘化这个操作，恰好抹平了不确定的那个方向。"],
    P("13-equivalence-class.png"),
)

# 14 清单 -----------------------------------------------------------------
gen.figure_card(
    F("fig-checklist"), "落地清单",
    ["按这条判据，常见分析可以直接分成两类。",
     "如上图左侧，单神经元调谐曲线、条件平均发放、试次平均的群体几何与解码，"
     "都是边缘的函数，在等价类上恒定，**可以跨 session 拼**；右侧的噪声相关、"
     "Granger 因果、跨区共享的潜在变异与 communication subspace，依赖联合表内部，"
     "随那个未知的自由度变化，**拼不了**。",
     "动手前问一句就够：我要的这个量，会不会随那个未知的自由度变化？"
     "那么专门为跨 session 设计的那批模型，处在什么位置？"],
    P("14-checklist.png"), figure_height=620,
)

# 15 stitching ------------------------------------------------------------
gen.figure_card(
    F("fig-stitch"), "跨 session 模型的位置",
    ["跨 session 建模这条线并不推翻上面的结论，它换了问题。",
     "如上图，只有观测时整条线都与数据相容；加上结构假设与锚点之后，候选被缩到很小的"
     "范围——**假设不改变观测，它缩小的是候选集合**。这条线从 LFADS² 起，经 NDT-2³、"
     "POYO⁴，到 MtM⁵ 与 POYO+⁶，逐步把跨 session 对应做成可学的结构。",
     "底部两块是它买到与买不到的东西：**买到**可在留出数据上验证的迁移与解码增益；"
     "**买不到**从未同时记录的那一对之间的协同波动。"],
    P("15-stitching.png"), figure_height=560,
)

# 16 / 17 尾卡 -------------------------------------------------------------
gen.tail_card(
    ["¹ Nelsen RB. (2006). An Introduction to Copulas, 2nd ed. Springer.",
     "² Pandarinath C, et al. (2018). Inferring single-trial neural population dynamics "
     "using sequential auto-encoders. Nature Methods 15:805–815.",
     "³ Ye J, et al. (2023). Neural Data Transformer 2: multi-context pretraining for "
     "neural spiking activity. Advances in Neural Information Processing Systems 36."],
    P("16-tail-a.png"),
    lead_paragraphs=[
        "这条判据不否定分次记录的价值，它划出的是分次记录能回答的问题范围。",
        "要避免的是把模型在不可辨识方向上的插补，写成从数据中得到的发现——"
        "这类错误在数据内部看不见，因为没有可对照的真值。当一个问题落在拼不了的那一类时，"
        "剩下两条路：把结论如实标注为「在某某假设下的推断」，或者去做同时记录。",
    ],
)

gen.tail_card(
    ["⁴ Azabou M, et al. (2023). A unified, scalable framework for neural population "
     "decoding. Advances in Neural Information Processing Systems 36.",
     "⁵ Zhang Y, et al. (2024). Towards a “universal translator” for neural dynamics at "
     "single-cell, single-spike resolution. Advances in Neural Information Processing "
     "Systems 37.",
     "⁶ Azabou M, et al. (2025). Multi-session, multi-task neural decoding from distinct "
     "cell-types and brain regions. ICLR 2025."],
    P("17-tail-b.png"),
)

print("done ->", OUT)
